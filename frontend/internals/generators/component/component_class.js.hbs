import React from 'react';
import PropTypes from 'prop-types';

class {{ name }} extends  {
  render() {
    return (
      <div>
      </div>
    );
  }
}

{{ name }}.propTypes = {
};

export default {{ name }};
